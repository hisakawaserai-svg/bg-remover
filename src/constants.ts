/**
 * constants.ts — 翻訳してはいけない識別子を置く場所。
 *
 * 【表示名と内部IDは必ず分ける】
 * ここに入れてよいのは「言語が変わっても絶対に変わってはいけない値」だけ。
 * 画面に出す文言は i18n のカタログ（src/i18n/ja.ts）に書く。
 *
 * アプリの表示名は t('app.name') を使う。ここには置かない
 * （以前は APP_NAME という定数で日本語がハードコードされており、
 *   英語表示にしてもヘッダーだけ「スタンプ抜き」のまま残っていた）。
 */

/**
 * 写真アルバムの内部ID。**表示名ではない。**
 *
 * CameraRoll.saveAsset({ album }) で書き出し先を指定し、
 * SavedScreen が groupName で検索するときの実体キー。
 * 写真アプリ側に実在するアルバム名そのものなので、
 * 変更・翻訳すると次の実害が出る:
 *   - 既存ユーザーの保存済みアルバムと結び付かなくなり、過去の画像を見失う
 *   - 新旧2つのアルバムに分裂して保存先が散らばる
 *
 * 「保存先アルバム」「アルバム名（内部）」など、ユーザーが写真アプリで
 * このアルバムを探すための表示にはこの値をそのまま出す。翻訳した名前を
 * 出すと、その名前のアルバムは写真アプリに存在しないので探せなくなる。
 */
export const ALBUM_ID = 'スタンプ抜き';
